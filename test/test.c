// int getint( );
// void putint(int x);

/*
const int ca = 0;
const int cb[5] = {1, 2, 3, 4, 5};

int a = 6;
int b[5] = {11, 8, 10, 7, 9};

int fun( ) {
	int t = 0;
	if (t > 0) {
		t = cb[t - ca + a];
		b[t] = getint( );
		t = t - b[t];
		return t * fun(t - 1);
	} else {
		return t;
	}
}

int main( ) {
	int k, i;
	int arr[5];

	a = getint( );
	arr[0] = getint( );
	arr[1] = getint( );
	arr[2] = getint( );

	arr[3] = getint( );
	arr[4] = getint( );

	i = arr[1];
	arr[1] = arr[i] - (10 + a) * a - 8;
	i = 0;

	while (i < 5) {
		if (i % 2 == 0) {
			k = k + cb[i];
		} else {
			k = k + b[i];
			// b[k] = getint( );
		}
		k = k + arr[i];
		if (k > 3) {
			k = k - a;
		} else {
			k = k - ca;
		}
		i = i + 1;
	}

	k = fun(k);
	putint(k);
}
*/

int b = 0;
int cc[3] = {5, 5, 5};
int main()
{
	int a = 0;
	int m = 0;
	int n = 100;
	int j = 11;
	int k = 100;
	a = m + n;
	return m;
}